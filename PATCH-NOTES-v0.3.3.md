# Battle Army Tools v0.3.3

## Updater repair
- Adds the canonical GitHub repository URL to `module.json`.
- Adds the stable `releases/latest/download/module.json` manifest URL.
- Adds the version-specific v0.3.3 release ZIP download URL.
- Preserves the v0.3.2 commander ownership and unit vision changes.
