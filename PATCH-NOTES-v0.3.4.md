# Battle Army Tools v0.3.4

- Fixes **Create Commander** button failing because its Team-options helper was scoped inside Deploy Units.
- Fixes **Manage Commanders** button failing because its commander-list helper was scoped inside Deploy Units.
- Updates the internal module version constant to 0.3.4.
- Preserves the raw GitHub manifest URL so future Foundry updates can resolve the manifest.
